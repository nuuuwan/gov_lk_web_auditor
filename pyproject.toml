[project]
name = "glwa"
version = "0.1.0"
description = "Evidence-based audits for Sri Lankan government websites"
requires-python = ">=3.11"
dependencies = [
  "cryptography>=45.0,<47",
  "httpx>=0.28,<1",
  "jsonschema>=4.25,<5",
  "openai>=1.0,<2",
  "playwright>=1.50,<2",
]

[tool.uv]
package = false
